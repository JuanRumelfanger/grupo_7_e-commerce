module.exports = (sequelize, DataTypes)=>{
    
    let alias = '';
    let cols = {

    }
    let config = {
        tableName:'',
       // timestamps:
    }
    //const .... = sequelize.define(alias, cols, config);
    
   // return ;
}