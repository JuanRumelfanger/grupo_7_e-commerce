<!DOCTYPE html>
<html lang="en">
<head>

    <%- include('../partials/head.ejs') %>

    <link rel="stylesheet" href="/css/stylesListProduct.css">

</head>
<body>
    <%- include('../partials/header.ejs') %>
    <%- include('../partials/search-bar.ejs') %>
    <main>
        <h1>Catálogo de Videojuegos</h1>

        <div class="products">
            <% products.forEach(product => { %>
                <div class="product">
                    <img src="/img/<%= products.images[0] %>" alt="<%= product.name %>">

                    <h2><%= products.name %></h2>

                    <p><%= products.description %></p>

                    <p>
                        <strong>Plataformas:</strong>
                        <%= products.platforms.join(', ') %>
                    </p>

                    <p>
                        <strong>Género:</strong>
                        <%= products.genre %>
                    </p>

                    <p>
                        <strong>Fecha de lanzamiento:</strong>
                        <%= products.releaseDate %>
                    </p>

                    <p>
                        <strong>Precio:</strong>
                        <%= products.price %>
                    </p>

                    <a href="/products/<%= products.id %>" class="btn">
                        Ver detalles
                    </a>

                </div>
            <% }) %>
        </div>
    </main>
    <%- include('../partials/footer.ejs') %>
</body>
</html>
